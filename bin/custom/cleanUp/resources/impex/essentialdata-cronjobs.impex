# Define the cron job and the job that it wraps
INSERT_UPDATE CleanUpCronJob; code[unique=true];job(code);singleExecutable;sessionLanguage(isocode)
                     ;cleanUpCronJob;cleanUpDirJobPerformable;false;de